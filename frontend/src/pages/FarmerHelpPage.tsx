import { ArrowLeft, ChevronDown, Headphones, Phone } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useState } from 'react'

const faqs = [{ question: 'How do I create a request?', answer: 'Choose New request, select the machine and area, add your field location, then send it to Seva CHC.' }, { question: 'What does each status mean?', answer: 'Pending means your CHC is reviewing it. Approved means a machine is available. Assigned means an operator has been chosen. In progress and Completed show the field work stage.' }, { question: 'How will I know about a change?', answer: 'FarmFleet sends an in-app notification when your request is approved, a job is assigned, a time changes, or the job is complete.' }, { question: 'Can I cancel a booking?', answer: 'You can cancel an Approved or Assigned booking before the operator starts the job.' }]
export function FarmerHelpPage() { const [open, setOpen] = useState(0); return <div className="dashboard farmer-page"><Link to="/farmer/dashboard" className="back-link"><ArrowLeft size={16} />Back to dashboard</Link><div className="page-intro"><div><span className="eyebrow">MY FARM / SUPPORT</span><h1>Help & support</h1><p>Simple answers for managing your field work.</p></div></div><div className="help-contact"><div className="help-icon"><Headphones size={22} /></div><div><strong>Need to speak with Seva CHC?</strong><span>Our dispatch team can help with a machine, route, or time.</span></div><a className="button button-secondary" href="tel:+911211234567"><Phone size={16} />Call CHC</a></div><div className="faq-list">{faqs.map((faq, index) => <div className={`faq-item ${open === index ? 'open' : ''}`} key={faq.question}><button onClick={() => setOpen(open === index ? -1 : index)}><strong>{faq.question}</strong><ChevronDown size={18} /></button>{open === index && <p>{faq.answer}</p>}</div>)}</div></div> }
