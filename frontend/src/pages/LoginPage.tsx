import { ArrowRight, Factory, LockKeyhole, MapPinned, Sprout } from 'lucide-react'
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../contexts/AuthContext'
import { useLanguage } from '../contexts/LanguageContext'
import { Button, Field, Input } from '../components/Ui'
export function LoginPage() { const { signIn, isLoading } = useAuth(); const { language, setLanguage, t, tr } = useLanguage(); const navigate = useNavigate(); const [email, setEmail] = useState('manager@farmfleet.in'); const [password, setPassword] = useState('password123'); const [error, setError] = useState(''); async function submit(e: React.FormEvent) { e.preventDefault(); setError(''); try { await signIn(email, password); navigate('/') } catch (err) { setError(err instanceof Error ? err.message : 'Could not sign in') } } return <div className="login-page"><div className="login-art"><div className="login-art-top"><span className="brand"><span className="brand-mark"><Factory size={20} /></span>Farm<span>Fleet</span></span><span className="art-stamp">SEVA CHC · 2025</span></div><div className="art-copy"><span className="eyebrow">{tr('OPERATIONS FOR THE FIELD')}</span><h1>{tr('Every acre.')}<br /><em>{tr('In motion.')}</em></h1><p>{tr('One clear view of the machines, people, and jobs that keep a CHC moving.')}</p></div><div className="field-graphic"><div className="sun" /><div className="furrow furrow-one" /><div className="furrow furrow-two" /><div className="furrow furrow-three" /><div className="tractor-art"><Sprout size={29} /></div><div className="plot-label label-one">{tr('PLOT 12B')}</div><div className="plot-label label-two">{tr('RABI / 03')}</div></div><div className="art-foot"><span><MapPinned size={15} />{tr('Meerut, Uttar Pradesh')}</span><span>{tr('Built for CHCs')}</span></div></div><main className="login-card"><div className="login-language"><span>{tr('Language')}</span><select aria-label="Select language" value={language} onChange={(event) => setLanguage(event.target.value as typeof language)}><option value="en">English</option><option value="hi">हिन्दी</option><option value="ta">தமிழ்</option></select></div><div className="login-heading"><span className="eyebrow">{t.welcome.toUpperCase()}</span><h2>{t.signIn}</h2><p>{t.tagline}.</p></div><form onSubmit={submit}><Field label={tr('Work email')}><Input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></Field><Field label={tr('Password')}><div className="password-field"><Input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /><LockKeyhole size={17} /></div></Field>{error && <div className="form-error">{error}</div>}<Button type="submit" className="login-submit" disabled={isLoading}>{isLoading ? tr('Signing you in...') : <>{t.openDesk} <ArrowRight size={17} /></>}</Button></form><div className="demo-note"><strong>{tr('Prototype access')}</strong><span>manager@farmfleet.in · password123</span></div><p className="login-legal">{tr("By continuing, you agree to FarmFleet's terms of use and data policy.")}</p></main></div> }
